<header class="main-header">
    <div class="logo-container">
        <img src="{{ url_for('static', filename='images/logo.jpg') }}" alt="Autoservis Logo" class="logo">
    </div>
    <nav class="main-nav">
        <ul class="nav-list">
            {% if 'role_id' in session and session['role_id'] == 1 %}
                <li class="nav-item"><a href="{{ url_for('sprava_uzivatele.sprava_uzivatelu') }}" class="nav-link">Správa uživatelů</a></li>
            {% endif %}

            {% if 'role_id' in session and session['role_id'] in [8, 6, 7, 5, 1] %}
                <li class="nav-item"><a href="{{ url_for('sprava_vozidel.spravaVozidel') }}" class="nav-link">Správa vozidel</a></li>
            {% endif %}

            {% if 'role_id' in session and session['role_id'] in [8, 1] %}
                <li class="nav-item"><a href="{{ url_for('planovani_kalendar.planovani_A_kalendar') }}" class="nav-link">Plánování a kalendář</a></li>
            {% endif %}

            {% if 'role_id' in session and session['role_id'] in [8, 1, 6, 7, 5, 3, 4, 2] %}
                <li class="nav-item"><a href="{{ url_for('stav.stav_uzivatele') }}" class="nav-link">Sledování stavu</a></li>
            {% endif %}

            {% if 'role_id' in session and session['role_id'] in [1, 3, 4, 2] %}
                <li class="nav-item"><a href="{{ url_for('zpravy_statistiky.statistiky') }}" class="nav-link">Zprávy a statistiky</a></li>
            {% endif %}
            <li class="nav-item"><a href="{{ url_for('odhlaseni.odhlas') }}" class="nav-link logout-btn">Odhlásit</a></li>
        </ul>
    </nav>
</header>
